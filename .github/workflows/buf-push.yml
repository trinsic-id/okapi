name: Push protos to buf.build
on:
  push:
    branches:
      - main
jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: bufbuild/buf-push-action@v1
        with:
          buf_token: ${{ secrets.BUF_BUILD_TOKEN }}