name: Build Python SDK

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "python/**"
      - "proto/**"
      - ".github/workflows/*python*"
  push:
    branches:
      - main

jobs:
  test-python:
    name: Test Python code
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: [ 3.6, 3.7, 3.8, 3.9 ]  # '3.10' has issues for now
    steps:
      - uses: actions/checkout@v2
      - name: Download workflow artifact
        uses: dawidd6/action-download-artifact@v2.14.0
        with:
          workflow: "build-libs.yml"
          path: ./libs
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Display Python version
        run: python -c "import sys; print(sys.version)"
      - name: Build, Test, Pack
        run: ./devops/BuildPython.ps1 -GitTag ${{ github.ref }} -TestOutput "test_output_${{ matrix.os }}.xml" -ArtifactName "${{ matrix.artifact_name }}"
        shell: pwsh
  #     - name: Upload Unit Test Results
  #       if: always()
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: Unit Test Results (Python ${{ matrix.python-version }})
  #         path: '**/python/test_output_*.xml'

  # publish-test-results-python:
  #   name: Publish Python Test Results
  #   needs: test-python
  #   runs-on: ubuntu-latest
  #   if: always()

  #   steps:
  #     - name: Publish Test Report - Python ${{ matrix.python-version }}
  #       uses: mikepenz/action-junit-report@v2
  #       with:
  #         report_paths: '**/python/test_output_*.xml'
  #         fail_on_failure: true
  #         check_name: Python Test Report
