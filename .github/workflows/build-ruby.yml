name: Build Ruby bindings

on:
  pull_request:
    paths:
      - "ruby/**"
      - "proto/**"
      - ".github/workflows/*ruby*"
  push:
    branches:
      - main

jobs:
  build_ruby:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]
        # Due to https://github.com/actions/runner/issues/849, we have to use quotes for '3.0'
        ruby-version: [ 2.7, '3.0' ]

    steps:
      - uses: actions/checkout@v2
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1.76.0
        with:
          ruby-version: ${{ matrix.ruby-version }}
      - name: Download workflow artifact
        uses: dawidd6/action-download-artifact@v2.14.0
        with:
          workflow: "build-libs.yml"
          path: ./libs
      - name: Build and run tests
        run: ./devops/BuildRuby.ps1 -GitTag ${{ github.ref }} -TestOutput "test_output_${{ matrix.os }}.xml" -ArtifactName "${{ matrix.artifact_name }}"
        shell: pwsh
      # - name: Publish Test Report - Ruby
      #   uses: mikepenz/action-junit-report@v2
      #   if: always() # always run even if the previous step fails
      #   with:
      #     report_paths: '**/ruby/coverage/*.xml'
      #     fail_on_failure: true
      #     check_name: Ruby Test Report