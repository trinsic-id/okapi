import {
  didcomm_pack,
  didcomm_sign,
  didcomm_unpack,
  didcomm_verify,
  didkey_convert,
  didkey_generate,
} from "./didcommgrpc";

export default {
  didcomm_pack,
  didcomm_sign,
  didcomm_unpack,
  didcomm_verify,
  didkey_convert,
  didkey_generate,
};
