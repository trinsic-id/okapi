{
  "name": "didcomm-proto",
  "version": "1.0.1",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc -p . & npm run copy",
    "copy": "npx copyfiles -u 1 ./src/proto/*.js ./src/proto/*.d.ts ./dist/",
    "test": "echo 'no tests'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "google-protobuf": "^3.14.0"
  },
  "devDependencies": {
    "@types/chai": "^4.2.14",
    "@types/mocha": "^8.0.3",
    "chai": "^4.2.0",
    "copyfiles": "^2.4.0",
    "mocha": "^8.2.1",
    "ts-mocha": "^8.0.0",
    "typescript": "^4.0.5"
  }
}
