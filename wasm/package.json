{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "npm install && npx lerna exec npm install",
    "build": "npx lerna exec npm run build",
    "test": "npx lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}
